# Shell scripts must always use LF so they run inside Linux containers.
# Windows CRLF endings break the shebang (env: 'bash\r': No such file or directory).
*.sh text eol=lf
entrypoint.sh text eol=lf
